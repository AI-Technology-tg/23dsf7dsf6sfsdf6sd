# Supabase (Re-Minko)

**Схема БД:** редактируйте только [`../database.sql`](../database.sql).

Папка `migrations/` — исторические снимки для CLI; новые изменения **не дублируйте** сюда, дописывайте в `database.sql`.

Проект: `ipsawgtsicxwkkkipchp` (Re - Minko).
