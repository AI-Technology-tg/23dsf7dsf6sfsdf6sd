# SQL Re-Minko

**Итоговая схема:** [`../database.sql`](../database.sql)

**Новые изменения:** временный файл в [`pending/`](pending/) → apply на Supabase → commit → перенос в `database.sql` → удалить pending.

См. `.cursor/rules/database-migration-workflow.mdc`.
